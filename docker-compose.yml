services:
  vlm-demo:
    build: .
    ports:
      - "7861:8080"
    env_file:
      - .env
    volumes:
      - ./flagged:/app/flagged
    restart: unless-stopped
    environment:
      - APP_PORT=7860

networks:
  vlm-network:
    driver: bridge
